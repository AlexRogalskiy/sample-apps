===============
Deploy on AWS
================

$ cld environment create env-2 environment-rds-ecs.yaml

$ cld app deploy greetings aws --env-id <env-id>

Enter application port number as 5000 when prompted

